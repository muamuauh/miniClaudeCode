__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.venv/
build/
dist/
.coverage
*.log
~/.miniclaudecode/sessions/

# Per-project session registry (machine-local; references global snapshots)
.miniclaudecode/sessions.json

# Eval run reports
evals/reports/

# Secrets -- the file may contain real API keys. The committed `.env.example`
# is the template.
.env
.env.local
